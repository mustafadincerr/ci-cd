version: '3'

services:
  backend:
    image: your_dockerhub_username/backend-app:latest
    ports:
      - '3000:3000'
    environment:
      - MONGODB_URI=mongodb://database:27017/mydb
    depends_on:
      - database

  database:
    image: mongo
    ports:
      - '27017:27017'
